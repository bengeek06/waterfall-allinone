# Fichier d'exemple des variables d'environnement
# Copiez ce fichier vers .env et personnalisez les valeurs

# =================================================================
# SECRETS DE SÉCURITÉ (optionnels - générés automatiquement si vides)
# =================================================================

# Secret JWT pour l'authentification (32-64 caractères recommandés)
# Exemple: openssl rand -hex 32
JWT_SECRET=

# Token d'authentification inter-services (32-64 caractères recommandés)  
# Exemple: openssl rand -hex 32
INTERNAL_AUTH_TOKEN=

# Mot de passe PostgreSQL pour l'utilisateur appdb
# Exemple: openssl rand -base64 32
POSTGRES_PASSWORD=

# =================================================================
# CONFIGURATION DES APIS FLASK (optionnel)
# =================================================================

# Environnement Flask (production, development, testing)
FLASK_ENV=production

# Niveau de logs (debug, info, warning, error, critical)
LOG_LEVEL=info

# =================================================================
# EXEMPLES DE GÉNÉRATION DE SECRETS SÉCURISÉS
# =================================================================

# Générer un JWT_SECRET :
# openssl rand -hex 32

# Générer un INTERNAL_AUTH_TOKEN :
# openssl rand -hex 32

# Générer un POSTGRES_PASSWORD :
# openssl rand -base64 32

# Ou utiliser un générateur en ligne sécurisé :
# https://www.random.org/passwords/
